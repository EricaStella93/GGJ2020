using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VideoSound : MonoBehaviour
{
  /*  public AudioClip rissaClip;
    public float[] timeBetweenClips;
    
    private AudioSource source;
    private int currentFrame;
    
    private void Start()
    {
        source = GetComponent<AudioSource>();
        source.clip = rissa1Clip;
        StartCoroutine(Sounds());
    }

    private IEnumerator Sounds()
    {
        source.Play(rissa1Clip);
        yield return new WaitForSeconds(timeBetweenClips[currentFrame]);
        currentFrame++;
        //source.PlayOneShot(rissa2Clip);


    }
      */  
}
